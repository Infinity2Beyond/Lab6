package clients.utility;

public enum processingCode {

/**
 * Enum of all processing codes.
 */
    OK,
    ERROR,
    OBJECT,
    UPDATE_OBJECT,
    SCRIPT,
    REMOVE_OBJECT,
    SORT_SPEED

}
